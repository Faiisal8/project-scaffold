services:
  app:
    build: .
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - sqlite_data:/data
    environment:
      SQLITE_PATH: /data/app.db

volumes:
  sqlite_data:

